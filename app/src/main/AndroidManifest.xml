<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo174"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo174"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClimateData">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ActivityUpdateDate" />
        <activity android:name=".ActivityUpdatePassword" />
        <activity android:name=".fragment_createSowing.CreateSowingActivity"/>
        <activity android:name=".fragment_createLocation.CreateLocation"/>
        <activity android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".RegisterActivity"/>
    </application>
</manifest>